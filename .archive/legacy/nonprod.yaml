apiVersion: v1
kind: ConfigMap
metadata:
  name: aks-ikv-nonprod-config
  namespace: argocd
  labels:
    cluster: ikv
    environment: nonprod
    argocd.argoproj.io/secret-type: cluster
data:
  cluster.name: "aks-ikv-nonprod"
  cluster.group: "ikv"
  environment: "nonprod"
  # Define which components to deploy on this cluster
  components: |
    - redir-operator
    - rabbitmq-operator
    - keda-operator
