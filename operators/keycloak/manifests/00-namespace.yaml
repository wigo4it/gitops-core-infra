# Keycloak namespace
# Note: Operator will be installed in this namespace
apiVersion: v1
kind: Namespace
metadata:
  name: keycloak-system
  labels:
    managed-by: argocd
    app.kubernetes.io/component: operator
